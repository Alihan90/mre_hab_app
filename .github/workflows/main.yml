name: Flutter Build APK

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy_apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.2' # Фіксуємо стабільну версію, яка точно збирала ваш проект без помилок Gradle
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK Release
        # Збірка під усі типи процесорів (включаючи ваш OPPO А17)
        run: flutter build apk --release --no-tree-shake-icons --target-platform android-arm,android-arm64

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: build/app/outputs/flutter-apk/app-release.apk
